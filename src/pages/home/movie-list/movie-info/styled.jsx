import {Grid, styled} from "@mui/material";
import {Link} from "react-router-dom";

export const ButtonBack = styled(Link)({
    textDecoration: 'none',
});

export const MovieDescription = styled(Grid)({
    display: 'flex'
})