import {Box, styled} from "@mui/material";

export const DetailsMovieBox = styled(Box)({
    width: '100%',
    paddingTop: '50px'
});
export const TabsInner = styled(Box)({
    borderBottom: 1,
    borderColor: 'divider'
});
